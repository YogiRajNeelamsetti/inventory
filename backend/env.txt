# Local shell export template (do not commit real secrets)
# Supabase-first defaults
export DB_URL=jdbc:postgresql://aws-1-<region>.pooler.supabase.com:5432/postgres?sslmode=require
export DB_USERNAME=postgres.<project-ref>
export DB_PASSWORD=<supabase-db-password>

# Optional local PostgreSQL fallback
# export DB_URL=jdbc:postgresql://localhost:5432/kirana_db
# export DB_USERNAME=postgres
# export DB_PASSWORD=your_local_db_password
export JWT_SECRET=replace-with-a-32-plus-char-random-secret
export GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
